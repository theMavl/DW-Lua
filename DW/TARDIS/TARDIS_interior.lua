script_name("TARDIS Interior")
script_author("Mavl Pond")
script_version("1.0")
script_version_number(1)
script_description("TARDIS Interior main script")

require "lib.moonloader"

local TARDIS_API = import('DW/TARDIS/TARDIS_main')
local labels = require("DW/sys/labels")
local T_mode = labels.TARDIS_mode


function main()
  wait(0)
  while true do
  	wait(0)
  end
end
